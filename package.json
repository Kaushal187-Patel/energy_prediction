{
  "name": "energy-ai",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm start\" \"cd ml-model && python predict_api.py\"",
    "build": "cd frontend && npm run build",
    "install-all": "cd frontend && npm install && cd ../backend && npm install && cd ../ml-model && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}